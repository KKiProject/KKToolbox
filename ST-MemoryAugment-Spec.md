# KKToolbox · Memory Augment 当前实现规范

## 1. 发布与运行方式

- 项目必须是可由 SillyTavern 通过 GitHub 地址直接安装的普通第三方扩展。
- `manifest.json`、`index.js` 和 `style.css` 位于仓库根目录。
- 不依赖 Server Plugin，不要求玩家修改配置文件，电脑与手机使用同一安装方式。
- `server-plugin/` 只保留为旧版兼容源码；新安装不复制、不启用、不调用它完成日常功能。
- API Key 只存在玩家自己的 SillyTavern 扩展设置中，不得写进源码、默认值或发布包。

## 2. 数据与 API 架构

### 2.1 外部 API

浏览器直接调用玩家填写的 OpenAI 兼容服务：

- `{base_url}/v1/models`
- `{base_url}/v1/embeddings`
- `{base_url}/v1/rerank`
- `{base_url}/v1/chat/completions`

Base URL 统一去掉末尾 `/v1` 和斜杠后再拼端点。Embedding 是 RAG 必需项；Reranker 与弹幕 API 可留空。服务商必须允许浏览器跨域访问。

任何增强功能失败都不得阻止 SillyTavern 的正常回复：

- 聊天向量失败：本轮不使用聊天召回。
- 世界书向量失败：本轮不使用世界书召回。
- Reranker 失败：退回该来源的向量排序，错误独立记录。
- 弹幕失败：只在弹幕面板显示错误。

### 2.2 本地存储

- 数字向量写入 SillyTavern 内置 `/api/vector/*`，使用客户端提供向量的 `webllm` source。
- 向量模型作用域包含 Base URL 与模型名签名，换模型后自动重建，避免不同维度混用。
- 可读文本、楼层、分段序号、角色、人工修改和禁用状态通过 `/api/files/*` 保存到当前用户文件区。
- 聊天按 `chat_id` 建立独立记录和向量集合。
- 世界书按 `book_id` 建立独立记录和向量集合。
- 文本记录不重复保存整串浮点向量，只保存向量 hash 与维度；数字向量由酒馆内置索引管理。

### 2.3 旧版迁移

- 仅当当前聊天的新记忆库为空时探测旧版 `/api/plugins/st-memory-augment/chat-memory`。
- 若旧服务存在，按原片段复制正文、楼层、分段、人工修改和禁用状态，并重新生成新索引。
- 迁移成功后本次会话不再重复探测。
- 不删除、覆盖旧服务端数据。
- 旧服务不存在时静默切换为纯前端模式。

## 3. 聊天向量记忆

- 每条酒馆消息独立分段，默认目标 400 个中文字符。
- 优先在句末或换行断开，再考虑逗号/分号；不足 100 字的尾段并入前段。
- 每个片段包含稳定 ID、楼层号、楼内序号、角色、正文、字数和时间。
- 最近 4 个已确认楼层作为缓冲，不提前向量化；打开聊天时也必须遵守此缓冲。
- swipe/regenerate 中的未确定 AI 回复不进入向量库。
- 删除楼层或从旧位置继续游玩时自动核对并清理普通孤立片段。
- 人工修改和人工禁用优先级最高，自动核对不得覆盖或重新启用它们。
- 人工修改正文后立即重建该片段向量；“恢复酒馆原文”才撤销人工修改。

记忆库界面：

- 默认只显示每条片段的标题行，点击后展开正文和操作。
- 列表区域有固定最大高度，超出后在记忆库内部滚动。
- 支持按正文或楼层搜索、分页加载、修改、禁用、启用和恢复原文。

## 4. RAG 召回与注入

- 查询使用最近 2–3 条有效消息。
- 十楼总结、聊天历史与世界书分开搜索、分开精排、分开计数，失败互不影响。
- 固定携带最新最多 5 条十楼总结；更早总结固定粗召回最多 10 条、最终最多 3 条。
- 进入本轮的全部总结共同圈定对应楼层的正文候选；聊天正文继续使用面板中的 top-K/top-N，并保留最多 3 条全库候选兜底。
- 世界书固定粗召回最多 10 条，最终最多 5 条。
- 数量均为上限：不足就按实际结果，0 条就不注入，不补齐。
- 搜索聊天历史时排除仍作为最近原文发送的楼层，避免重复和时间线倒退。
- Reranker 未配置时保留向量顺序。
- 精排阈值只在配置 Reranker 后生效。

剧情总结与聊天召回合并为“历史上下文参考”，放在 `[Start a new Story]` 后、正式聊天记录前；模板没有该标记时放在第一条聊天记录前：

- 片段可能相关，也可能无关。
- 仅供参考，不要求必须采用。
- 真正相关时应用于保持人物认知、事实与因果连续。
- 不得把历史片段当成当前场景，不得导致时间线倒退。
- 与最近原文冲突时，以最近原文和较晚楼层为准。

世界书召回用 `[设定召回-世界书名]` 标记，保持独立，不与剧情历史召回混用。

## 5. 世界书语义向量

- 不自动把所有世界书向量化，只处理玩家手动勾选的书或条目。
- 整本勾选表示该书所有有效条目；逐条勾选只处理指定条目。
- 世界书内容更新后，只同步当前被选择的范围。
- 语义召回不改变酒馆原有关键词、常驻、禁用或插入位置规则。

## 6. 自动总结

- 使用弹幕同一套副 API，不调用正文主 API。
- 面板字段为“保留最近 N 楼”和“单次总结楼层数”；保留范围最大 30 楼。
- 只有未总结消息离开保留窗口且积满一批时才总结。
- 每个角色使用独立的 `[角色名]-自动总结` 世界书。
- 十楼详细总结为绿色关键词条目，不由酒馆常驻注入；插件固定携带最新最多 5 条，并对更早总结做独立语义召回。
- 第一次生成详细总结时同时建立蓝色常驻的“历史概括”空条目；每累计 5 条详细总结追加一个带真实楼层范围的阶段概括，不重新压缩旧概括。
- 写入成功后用 SillyTavern 原生隐藏功能隐藏已总结旧楼层。
- 生成拦截器不裁剪原始消息，只向本次生成副本注入召回结果。

## 7. 弹幕

- 使用独立 Chat Completions API。
- 最近消息是“前情回顾”，RAG 是“相关记忆”，最新 AI 回复是唯一要评论的“最新章节”。
- 弹幕缓存只放在聊天元数据，不写入 `context.chat`，不进入后续上下文。
- swipe 后重新生成，旧异步结果不得覆盖新版本。

## 8. 界面与容错

- 顶部入口使用工具箱图标，位于用户人设（笑脸）按钮左侧。
- 快捷面板高度随内容收缩；大区块使用折叠面板。
- API 卡片在窄屏改为单列，控件不得重叠或挤成竖排。
- “拉取模型”位于各 API 卡片右下角。
- 状态只显示当前聊天的向量片段数与估算大小，不显示所有角色、所有聊天的总库大小。
- 空记忆库、空世界书、0 个召回结果都属于正常状态。
